import 'package:flutter/widgets.dart';

class ColorPalette {
  // Gradients
  static const List<Color> backgroundGradient = [
    Color(0xFFE6F2FF), // Light blue
    Color(0xFFE6E6FA), // Lavender
  ];

  static const List<Color> headerGradient = [
    Color(0xFF3B82F6), // Bright blue
    Color(0xFF7C3AED), // Deep purple
  ];

  static const List<Color> buttonGradient = [
    Color(0xFF2563EB), // Strong blue
    Color(0xFF9333EA), // Vibrant purple
  ];

  static const List<Color> buttonHoverGradient = [
    Color(0xFF1D4ED8), // Slightly darker blue
    Color(0xFF7E22CE), // Slightly darker purple
  ];

  // Text colors
  static const Color primaryText = Color(0xFF1F2937); // Dark gray for main text
  static const Color secondaryText = Color(0xFF4B5563); // Slightly lighter gray
  static const Color whiteText = Color(0xFFFFFFFF); // White
}
